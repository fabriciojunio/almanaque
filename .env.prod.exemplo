# As variáveis que a publicação sem servidor precisa.
#
# APP_ENV=prod
# APP_DEBUG=0
# APP_SECRET=<32 hexadecimais>
# DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require&serverVersion=17&charset=utf8
# ELASTICSEARCH_URL=          (vazio: a busca usa o banco)
